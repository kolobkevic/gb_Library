spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:library;MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect